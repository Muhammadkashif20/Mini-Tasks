var now=new Date()
console.log(now);

var currentDate=now.getMonth()+'-'+now.getDate()+'-'+now.getFullYear()
console.log(currentDate);
var currentDate=now.getMonth()+'/'+now.getDate()+'/'+now.getFullYear()
console.log(currentDate);
